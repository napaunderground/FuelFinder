package fuelfinder.mann.Utility;


/**
 * Created by nathan on 3/31/15.
 */
public class DataStorageManager {

/*
    public void saveToFile(String filename) {

        SharedPreferences newInput = null;

        String y = MileageModel.getYear();
        String ma = MileageModel.getMake();
        String mo = MileageModel.getModel();
        String m = MileageModel.getUserMileage();
        String e = MileageModel.getEngine();
        String t = MileageModel.getTransmission();
        SharedPreferences.Editor editor = newInput.edit();

        editor.putString(filename + " YearOfMfr", y);
        editor.putString(filename + " Manufacturer", ma);
        editor.putString(filename + " VehModel", mo);
        editor.putString(filename + " FuelMileage", m);
        editor.putString(filename + " EngineSize", e);
        editor.putString(filename + " Transmission", t);


        editor.commit();
    }
*/
}